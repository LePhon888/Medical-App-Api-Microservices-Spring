package com.med.utils;

/**
 * This enum class will hold the screen names related to front end
 * Use when need to send a notification with click action that navigate to specific screen
 */
public enum Screen  {
    MedicationBox;
   public String getName() {
        return name();
   }
}
